diff --git a/src/simple_stats.h b/src/simple_stats.h
--- a/src/simple_stats.h
+++ b/src/simple_stats.h
@@ -18,6 +18,10 @@ class SimpleStats {
     // incrementing counter
     void Increment(const std::string name) { epoch_counters_[name] += 1; }

+    void IncrementBy(const std::string name, uint64_t value) {
+        epoch_counters_[name] += value;
+    }
+
     // incrementing for vec counter
     void IncrementVec(const std::string name, int pos) {
         epoch_vec_counters_[name][pos] += 1;
diff --git a/src/simple_stats.cc b/src/simple_stats.cc
--- a/src/simple_stats.cc
+++ b/src/simple_stats.cc
@@ -25,6 +25,18 @@ SimpleStats::SimpleStats(const Config& config, int channel_id)
     InitStat("num_refb_cmds", "counter", "Number of REFb commands");
     InitStat("num_srefe_cmds", "counter", "Number of SREFE commands");
     InitStat("num_srefx_cmds", "counter", "Number of SREFX commands");
+    InitStat("read_issue_first_cycle", "counter",
+             "Cycle of the first issued READ command");
+    InitStat("read_issue_span_cycles", "counter",
+             "Cycles from first to last issued READ command");
+    InitStat("read_issue_gap_cycles", "counter",
+             "Sum of gaps between consecutive issued READ commands");
+    InitStat("read_issue_tccd_s_pairs", "counter",
+             "Consecutive same-rank READ pairs to different bank groups");
+    InitStat("read_issue_tccd_l_pairs", "counter",
+             "Consecutive same-rank READ pairs to the same bank group");
+    InitStat("read_issue_cross_rank_pairs", "counter",
+             "Consecutive READ pairs to different ranks");
     InitStat("hbm_dual_cmds", "counter", "Number of cycles dual cmds issued");

     // double stats
diff --git a/src/controller.h b/src/controller.h
--- a/src/controller.h
+++ b/src/controller.h
@@ -66,6 +66,11 @@ class Controller {
     // used to calculate inter-arrival latency
     uint64_t last_trans_clk_;

+    bool read_issue_seen_;
+    uint64_t last_read_issue_clk_;
+    int last_read_issue_rank_;
+    int last_read_issue_bankgroup_;
+
     // transaction queueing
     int write_draining_;
     void ScheduleTransaction();
diff --git a/src/controller.cc b/src/controller.cc
--- a/src/controller.cc
+++ b/src/controller.cc
@@ -27,5 +27,9 @@ Controller::Controller(int channel, const Config &config, const Timing &timing)
                           : RowBufPolicy::OPEN_PAGE),
       last_trans_clk_(0),
+      read_issue_seen_(false),
+      last_read_issue_clk_(0),
+      last_read_issue_rank_(-1),
+      last_read_issue_bankgroup_(-1),
       write_draining_(0) {
     if (is_unified_queue_) {
         unified_queue_.reserve(config_.trans_queue_size);
@@ -230,6 +234,25 @@ void Controller::IssueCommand(const Command &cmd) {
 #endif  // THERMAL
     // if read/write, update pending queue and return queue
     if (cmd.IsRead()) {
+        if (!read_issue_seen_) {
+            simple_stats_.IncrementBy("read_issue_first_cycle", clk_);
+            read_issue_seen_ = true;
+        } else {
+            const uint64_t gap = clk_ - last_read_issue_clk_;
+            simple_stats_.IncrementBy("read_issue_span_cycles", gap);
+            simple_stats_.IncrementBy("read_issue_gap_cycles", gap);
+            if (cmd.Rank() != last_read_issue_rank_) {
+                simple_stats_.Increment("read_issue_cross_rank_pairs");
+            } else if (cmd.Bankgroup() == last_read_issue_bankgroup_) {
+                simple_stats_.Increment("read_issue_tccd_l_pairs");
+            } else {
+                simple_stats_.Increment("read_issue_tccd_s_pairs");
+            }
+        }
+        last_read_issue_clk_ = clk_;
+        last_read_issue_rank_ = cmd.Rank();
+        last_read_issue_bankgroup_ = cmd.Bankgroup();
+
         auto num_reads = pending_rd_q_.count(cmd.hex_addr);
         if (num_reads == 0) {
             std::cerr << cmd.hex_addr << " not in read queue! " << std::endl;
